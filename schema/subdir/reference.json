{
    "type": "object",
    "title": "Reference in SUBDIR",
    "description": "This ensures that subdir reference is used",
    "properties": {
        "subdir_param": {
            "title": "A simple parameter from SUBDIR",
            "type": "string"
        }
    }
}